import { Component, Input, OnChanges } from '@angular/core';
import { HttpClient, HttpHeaders } from '@angular/common/http';
interface Reminder { residentName:string; unitNumber:string; billReference:string; outstandingAmount:number; daysOverdue:number; reminderSent:boolean; }
@Component({selector:'app-reminders',standalone:true,template:`<section class="panel"><h2>Overdue payment reminders</h2><p>Review every overdue resident before sending reminders.</p><button (click)="send()" [disabled]="sending">{{sending?'Sending...':'Send overdue reminders'}}</button>@if(message){<p class="message">{{message}}</p>}<table><thead><tr><th>Resident</th><th>Unit</th><th>Bill</th><th>Outstanding</th><th>Days overdue</th><th>Reminder status</th></tr></thead><tbody>@for(item of reminders;track item.billReference+item.residentName){<tr><td>{{item.residentName}}</td><td>{{item.unitNumber}}</td><td>{{item.billReference}}</td><td>RM {{item.outstandingAmount.toFixed(2)}}</td><td>{{item.daysOverdue}}</td><td>{{item.reminderSent?'Sent':'Not sent'}}</td></tr>}</tbody></table></section>`,styles:[`.panel{margin-top:28px;padding:22px;background:#fff;border:1px solid #e4ddd7;border-radius:12px}.panel h2{margin-top:0}.panel button{padding:11px 16px;border:0;border-radius:7px;background:#bd613f;color:#fff;font-weight:700}.message{color:#276738;font-weight:700}table{width:100%;border-collapse:collapse;margin-top:20px}th,td{text-align:left;padding:13px;border-top:1px solid #e9e2dc}th{color:#736963}`]})
export class RemindersComponent implements OnChanges {@Input({required:true}) token='';reminders:Reminder[]=[];sending=false;message='';constructor(private http:HttpClient){}ngOnChanges(){this.load()}headers(){return new HttpHeaders({Authorization:`Bearer ${this.token}`})}load(){if(this.token)this.http.get<Reminder[]>('http://localhost:5112/api/admin/reminders/overdue',{headers:this.headers()}).subscribe(data=>this.reminders=data)}send(){this.sending=true;this.http.post<{remindersSent:number}>('http://localhost:5112/api/admin/reminders/overdue',{}, {headers:this.headers()}).subscribe({next:result=>{this.message=`${result.remindersSent} reminder(s) sent.`;this.sending=false;this.load()},error:()=>{this.message='Unable to send reminders.';this.sending=false}})}}
