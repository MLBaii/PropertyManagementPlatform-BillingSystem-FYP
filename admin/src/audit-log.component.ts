import { Component, Input, OnChanges } from '@angular/core';
import { HttpClient, HttpHeaders } from '@angular/common/http';
interface Audit { adminUsername:string; actionType:string; affectedEntity:string; affectedEntityId?:number; description?:string; timestamp:string; }
@Component({selector:'app-audit-log',standalone:true,template:`<section class="panel"><div class="head"><div><h2>Audit log</h2><p>Read-only record of admin actions.</p></div></div><table><thead><tr><th>Time</th><th>Admin</th><th>Action</th><th>Record</th><th>Description</th></tr></thead><tbody>@for(log of logs;track log.timestamp+log.description){<tr><td>{{log.timestamp}}</td><td>{{log.adminUsername}}</td><td>{{log.actionType}}</td><td>{{log.affectedEntity}}{{log.affectedEntityId?' #'+log.affectedEntityId:''}}</td><td>{{log.description}}</td></tr>}</tbody></table></section>`,styles:[`.panel{margin-top:28px;padding:22px;background:#fff;border:1px solid #e4ddd7;border-radius:12px}.head{margin-bottom:18px}.head h2{margin:0 0 6px}.head p{margin:0;color:#736963}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:13px;border-top:1px solid #e9e2dc}th{color:#736963;font-size:.82rem;text-transform:uppercase}td:first-child{font-family:monospace;font-size:.85rem}`]})
export class AuditLogComponent implements OnChanges {@Input({required:true}) token='';logs:Audit[]=[];constructor(private http:HttpClient){}ngOnChanges(){if(this.token)this.http.get<Audit[]>('http://localhost:5112/api/admin/audit-log',{headers:new HttpHeaders({Authorization:`Bearer ${this.token}`})}).subscribe(data=>this.logs=data)}}
