# PROGRESS LOG — PropertyBill

## How to use this file
Update this every working session. This is the **source of truth between sessions**
and the guard against code drifting from the Chapter 4 design spec. Before adding a
new feature, check what's already built vs. planned. If the code must diverge from
the Ch.4 spec, record it here so it can be disclosed in Chapter 5/7 rather than
discovered in the viva.

## Build order (from §3.1.2)
Auth → Profile → Bills → Dashboard → PDF → Payment Proof → Notifications → Disputes + Receipts

## Log

| Date | Module | What changed | Notes |
|------|--------|--------------|-------|
| YYYY-MM-DD | — | Repo scaffolded | Initial structure + docs |
| 2026-07-08 | Backend | Switched database from SQL Server to Supabase (PostgreSQL); kept ASP.NET Core API. | Reason: free online SQL Server hosting unavailable (Azure student region-restricted, Somee impractical). |
| 2026-07-08 | Backend | Scaffolded ASP.NET Core (.NET 9) Web API in `backend/` with EF Core + Npgsql provider. Layered structure: Controllers / Services / Repositories / Models / Data. Created all 15 entities from `docs/SCHEMA.md` (incl. `UnitBillingRate`, which the schema lists as admin-owned but wasn't in the original ask — added for consistency with the doc) with the Account→Unit and AdminUser corrections applied. Added `AppDbContext` with full relationship config. Configured JWT bearer auth issuing `ResidentId`/`UnitId`/`Role` claims, 60-min expiry. Sample endpoints: `POST /api/auth/resident/login` (stub JWT, no DB lookup yet) and `GET /api/residents/bills` (`[Authorize]`, scoped by the JWT's `UnitId` claim). Created `InitialCreate` EF Core migration — **not applied**. | Connection string placeholders only (`[YOUR-PASSWORD]` in `appsettings.json`, a local-only placeholder in the gitignored `appsettings.Development.json`) — fill in the real Supabase connection string locally, then run `dotnet ef database update` from `backend/`. |
| 2026-07-08 | Backend | Applied `InitialCreate` migration to the live Supabase Postgres database (all 15 tables + FKs + indexes created). | Direct DB host (`db.<ref>.supabase.co`) is IPv6-only and this network has no IPv6 route — switched `DefaultConnection` to the Session pooler host (`aws-0-ap-northeast-1.pooler.supabase.com:5432`, username `postgres.<project-ref>`) instead. Worth remembering for any future teammate on a similar network. |
| 2026-07-08 | Backend | Implemented real resident authentication (UC-101), replacing the login stub. Added `BCrypt.Net-Next` for password hashing; `Resident.IsActive` column added via `AddResidentIsActive` migration (applied). `POST /api/auth/resident/login` now looks up the resident by email (`ResidentRepository`), verifies the bcrypt hash, returns 401 on bad credentials, 403 if `IsActive` is false, and otherwise issues a real JWT (`ResidentId`/`UnitId`/`Role`, 60-min expiry) plus basic resident info. `LoginResponse` DTO now carries `residentId`/`fullName`/`email`/`unitId` alongside the token. Added a dev-only `DbSeeder` (idempotent, guarded behind `dotnet run -- --seed`) that inserts Skyview Residence, 2 units, 2 accounts, 2 residents (bcrypt password `Test1234`), and 2 bills + line items for one resident. Seeder run against Supabase; login + the `/api/residents/bills` flow verified end-to-end via curl (valid login → 200 + token, wrong password → 401, unknown email → 401, bills endpoint scoped correctly by the token's `UnitId`, rejects unauthenticated requests with 401). | `Test1234` is a seeded dev-only password (Alice Tan `alice.tan@example.com`, Benjamin Lee `benjamin.lee@example.com`) — never use it outside local/dev testing. |
| 2026-07-08 | Resident | Initialised the Expo app in `resident/` from scratch (managed workflow, TypeScript, Expo Router, React 19 / RN 0.86 / Expo SDK 57) — previously only a README existed. File-based routing: `(auth)/login` (unauthenticated) and `(tabs)/{home,bills,pay,alerts,profile}` (authenticated), gated by an `AuthContext` that checks `expo-secure-store` on launch and a root `index.tsx` redirect. Installed `expo-secure-store`, `axios`, `@expo/vector-icons`, `@expo-google-fonts/fraunces` + `@expo-google-fonts/inter`. Added `src/theme/` (colors.ts, typography.ts) encoding the warm dark editorial palette (`#1A1614` bg / `#221C18` surface / `#D67D5C` terracotta accent / Fraunces headings / Inter body) so every screen pulls from one source. Built the axios client (`src/services/api/client.ts`) with a request interceptor that attaches the stored JWT to every call, and an `authService.login()` that calls `POST /api/auth/resident/login`, stores the JWT + resident info via SecureStore, and is consumed through `AuthContext`. Built the Login screen matching Figure 4.8 of the mockups (logo badge, "Welcome back", email/password fields with inline validation, terracotta primary button, Forgot Password link, 401→"Invalid email or password", 403→disabled-account message). Home tab is a placeholder showing "Welcome, [resident name]" from the authenticated session, proving the login round-trip; Bills/Pay/Alerts/Profile are placeholders pending their own use cases. Verified: `npx tsc --noEmit` clean, `npx expo start --web` bundles 1030 modules with no errors. | Base URL is `EXPO_PUBLIC_API_BASE_URL` (see `resident/.env.example` — copy to `.env`, gitignored); differs for Android emulator (`10.0.2.2`), iOS simulator (`localhost`), and physical device (dev machine's LAN IP, both on same Wi-Fi). Backend's `launchSettings.json` binds to `localhost` only — testing from a physical device or emulator will additionally need `dotnet run --urls http://0.0.0.0:5112` so Kestrel accepts non-loopback connections; not changed yet since this session's testing was via `localhost`/web. |
| 2026-07-08 | Backend | Rebuild schema to match Chapter 4 ERD exactly — the previous schema had simplified several entities/relationships away from the documented design. Rolled back the two prior migrations (`dotnet ef database update 0`, which cleanly dropped all Supabase tables via each migration's `Down()`), deleted the old `Migrations/` folder, and rewrote all 15 entity classes column-for-column against the ERD given for this task. Structural changes of note: **Account moved from Unit-owned to Resident-owned** (`Resident.AccountId` FK, unique index enforcing 1—1; `Account` itself now holds only `CumulativeArrears`/`CreditBalance`/`LastUpdated`, no FK of its own); **Bill now references Unit directly** (`Bill.UnitId`) instead of via Account, and gained `BillingPeriod`/`ReferenceNumber`/`IssueDate`/`OutstandingBalance`; **PaymentProof now belongs to Resident** (`ResidentId` FK) rather than Bill, and `Payment` gained a nullable `ProofId` FK → PaymentProof (`SetNull` on delete, not cascade, per the task's explicit ask to avoid Payment/PaymentProof forming a hard mutual dependency); `BillingItem` gained a required `PropertyId` FK plus `Frequency`/`BillingDay`/`DueDay`/`PenaltyRate`/`GracePeriodDays`/`IsActive`; `AdminUser` gained `Username` (unique); `AuditLog` restructured to `ActionType`/`AffectedEntity`/`AffectedEntityId`/`Description`. Three entities (`BillLineItem.LineItemId`, `PaymentProof.ProofId`, `NotificationToken.TokenId`) needed explicit `HasKey()` calls since their PK names don't follow EF Core's `{EntityName}Id` convention. Updated `BillRepository`/`BillService`/`BillDto` (bills now query by `Bill.UnitId` directly, no more `Bill.Account.UnitId` join) and `AuthService` (`resident.Name` → `LoginResponse.FullName`, DTO field name kept stable for the frontend). Rewrote `DbSeeder` for the new columns. Created a fresh `InitialCreate` migration and applied it to Supabase; re-ran the seeder; re-verified login (200 + JWT on valid creds, 401 on bad password) and `/api/residents/bills` (200, correct bills with the new `referenceNumber`/`billingPeriod`/`outstandingBalance` fields) end-to-end via curl. Updated `docs/SCHEMA.md` to match exactly, replacing the now-obsolete "corrections vs Ch.4 ERD" section from the earlier simplified build. | `Resident.IsActive` is **not in the ERD** — kept per this task's explicit instruction, since UC-101 login's 403 (disabled account) case has no other ERD column to express it. Also note: `Resident.AccountId` unique index means the seeder must create each `Account` together with its `Resident` (no shared accounts across residents) — matches the current 1-resident-per-unit seed data but would need revisiting if co-owner/tenant units are seeded later. |
| 2026-07-08 | Both | Implemented the Profile feature (UC-102) end-to-end — backend and frontend. **Backend**: added `GET /api/residents/profile` (returns `Name`/`Email`/`PhoneNumber`/`NotificationPreferences` plus read-only `UnitNumber`/`Floor`/`PropertyName` via `Resident.Unit.Property`), `PUT /api/residents/profile` (updates Name/PhoneNumber/Email; `[EmailAddress]`/`[Required]` validation gives automatic 400s; checks for a duplicate email on another resident → 409; unit fields are not accepted), `PUT /api/residents/profile/password` (bcrypt-verifies `CurrentPassword`, 401 if wrong; `[MinLength(8)]` on `NewPassword`), and `PUT /api/residents/profile/notifications` (persists a `NotificationPreferencesDto` — `PushEnabled`/`EmailEnabled`/`BillDueReminders` — as JSON into the existing `NotificationPreferences` column). Followed Controller → Service (`ProfileService`, typed `ProfileResult`/`ChangePasswordResult` per the `AuthResult` convention already in the codebase) → Repository (extended `IResidentRepository` with `GetByIdAsync` (eager-loads Unit+Property), `ExistsWithEmailAsync`, `SaveChangesAsync`). All four endpoints verified end-to-end via curl against the live Supabase data for Alice Tan: profile fetch, phone update, invalid-email 400, duplicate-email 409, wrong-current-password 401, too-short-new-password 400, successful password change (confirmed old password then stops working and new one logs in), and notification preferences round-trip — then reset her seeded phone/preferences back to their original values afterward. **Frontend**: rebuilt the Profile tab to match Figure 4.15 exactly (avatar with initial, name, "Unit X · Property" line, editable Contact Information fields gated behind an Edit/Save/Cancel flow, read-only Unit Details card, and a settings-link card for Notification Preferences / Change Password / Log Out with the destructive action in the mockup's overdue-red). Notification Preferences and Change Password open as themed modal sheets (no dedicated mockup screen exists for either, so they extend the existing design system — toggle switches for the former, three password fields with client-side match/length checks for the latter) rather than as new tab-bar routes, since Figure 4.15 shows them as flyouts from the settings-link rows, not separate tabs. Added `profileService.ts` (typed API calls), `Avatar`/`Card`/`SettingsLinkRow`/`ModalSheet` UI components, and `colors.success`/`successBg` to the theme (lifted from the mockup's `--paid` status color, since the design system had no existing "success confirmation" color). Updated `docs/API-CONTRACT.md` with all four request/response shapes. Verified: `npx tsc --noEmit` clean, `npx expo start --web` bundles 1037 modules with no errors. | Backend had a stray `dotnet run` process left over from earlier manual testing that locked the build output (`apphost.exe`) — had to kill it before `dotnet build` would succeed; worth remembering to stop background API processes after manual curl testing sessions. |
| 2026-07-09 | Both | Implemented the Bills feature (UC-103) end-to-end — list + detail screens. **Backend**: `GET /api/residents/bills` now accepts an optional `?status=` filter (case-insensitive, matches the *computed* status) and every bill in the response carries a computed `status` (`Unpaid`/`Overdue`/`Paid`, `ProofSubmitted` once that exists) and `daysUntilDue` — `BillService.ComputeEffectiveStatus` derives "Overdue" from `dueDate` vs. the current date rather than trusting the raw stored column, since nothing in this project runs a scheduled job to flip a bill's status when it lapses (raw `Bill.Status` now only ever holds `"Unpaid"`/`"Paid"`, renamed from the old `"Pending"` value for vocabulary consistency with the mockups and this feature's filter values). Added `GET /api/residents/bills/{billId}` (`BillDetailDto` with the full `lineItems` breakdown) via a new `IBillRepository.GetByIdForUnitAsync(billId, unitId)` that filters by both IDs in one query — a bill belonging to another resident's unit simply doesn't match, so "doesn't exist" and "isn't yours" are indistinguishable by design (uniform 404, chosen over a separate 403 to avoid confirming a `billId` belongs to someone else). Added a `--reseed` flag (`DbSeeder.ClearAsync` + `SeedAsync`) since the seeder's existing idempotency check meant the new seed data wouldn't appear in the already-seeded Supabase database otherwise; used it to add a third bill for Alice (period 2026-04, overdue, 7 line items including "Outstanding b/f" and "Late Interest (1%)") that mirrors Figure 4.11's example numbers exactly (RM 420.50 total) so the Bill Detail screen has real data to match against. All endpoints verified end-to-end via curl: full list (3 bills, correct computed statuses/countdowns), `?status=Overdue` and `?status=overdue` (case-insensitivity), `?status=Paid`, full line-item detail for the overdue bill, 404 on a nonexistent bill, 404 on Benjamin requesting Alice's bill (cross-resident block), 401 with no token, and Benjamin's own empty bill list (no bills seeded for him — useful for the frontend's empty state). **Frontend**: built the Bill List screen (`(tabs)/bills/index.tsx`) matching Figure 4.10 — eyebrow bill count, filter row (All/Unpaid/Overdue/Paid, per this task's explicit 4-filter instruction rather than the mockup's 5 including "Proof Submitted," which has no feature to produce that state yet), and bill cards (reference, colour-coded `StatusBadge`, period, total, and a countdown that reads "N days overdue"/"N days left"/"Due today"/"Settled"/"Pending review" depending on status) with loading/error/empty states (distinct empty messages for "no bills at all" vs. "no bills matching this filter"). Restructured `(tabs)/bills.tsx` into a `bills/` folder (`_layout.tsx` Stack + `index.tsx` + `[billId].tsx`) — the standard Expo Router pattern for a tab that pushes its own nested screens — so tapping a card navigates to the Bill Detail screen (Figure 4.11: back-to-Bills link, status badge, reference, billing period, combined countdown+due-date line, itemised `Card` with the late-interest line item recoloured to the overdue/danger colour, and a bold "Total Due" row) with the Upload Payment Proof / Download PDF / Dispute This Bill actions rendered as disabled placeholders per this task's explicit instruction not to wire them yet. Added `colors.unpaid`/`unpaidBg`, `colors.pending`/`pendingBg`, and `colors.surface2` to the theme (all lifted directly from the mockup's CSS variables) plus the JetBrains Mono font (`@expo-google-fonts/jetbrains-mono`) for reference numbers, amounts, and countdown text, matching the mockup's `--font-mono` usage exactly. Verified: `npx tsc --noEmit` clean, `npx expo start --web` bundles 1048 modules with no errors. | Same stray-`dotnet run`-process build lock as the Profile feature — killed it before `dotnet build` would succeed again. Also: `BillingItem`/`UnitBillingRate` tables are still unseeded (no admin billing-item catalogue exists yet), so `BillLineItem.BillingItemId` stays null for every seeded line item — the Description/Amount/LineItemType fields alone are enough for UC-103's display needs. |
| 2026-07-09 | Resident | Implemented session timeout handling (UC-101 A6) after diagnosing a stuck-session bug: `expo-secure-store` persists across app reloads and even Metro cache clears, so an old token from a previous test session was being silently resumed on launch by `AuthContext` — once it passed its 60-minute expiry, every authenticated call (Profile, Bills) returned an empty-body 401 with no indication why, since nothing decoded the token or checked its age. Added `src/utils/jwt.ts` (`isTokenExpired`, decodes the JWT payload client-side without a library — no signature verification, that's still the backend's job on every request) and wired it into `secureStorage.loadSession()`: an expired stored token is now cleared and treated as "no session" rather than resumed. Added a global axios response interceptor (`client.ts`) that, on any 401 *except* from the login endpoint itself (whose 401s mean "wrong password" and are handled inline by the Login screen), clears the stored session and redirects to `/(auth)/login?sessionExpired=1`, which the Login screen reads to show "Your session has expired. Please log in again." in the same error slot used for login failures. Since the interceptor is a module-level singleton outside the React tree, added `sessionEvents.ts` (a tiny pub/sub) so it can also clear `AuthContext`'s in-memory `resident` state — without this, the `(auth)`/`(tabs)` route guards would still think the user was logged in until the next app launch, even after the token was cleared from storage. Extracted `AUTH_TOKEN_KEY`/`RESIDENT_INFO_KEY` into a new `storageKeys.ts` so `client.ts` could safely import `clearSession` from `secureStorage.ts` without a circular dependency (previously `secureStorage.ts` imported the token key from `client.ts`). Removed all temporary diagnostic `console.log`/`console.error` calls added during the earlier debugging session (`client.ts`, `secureStorage.ts`, `profileService.ts`, `billsService.ts`, `profile.tsx`, `bills/index.tsx`) now that the real bug is understood and fixed. Verified: `npx tsc --noEmit` clean, `npx expo start --web` bundles 1051 modules with no errors (confirms no circular-import breakage from the storageKeys refactor). | The interceptor's login-endpoint exclusion matters: without it, a wrong-password attempt on the Login screen itself would also trigger the "session expired" redirect/clear logic on top of the screen's own 401 handling — harmless in effect (already on Login) but semantically wrong and would show a misleading message. |
